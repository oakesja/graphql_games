{
  "name": "graphql_games_backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "DATABASE_NAME=games_test jest",
    "testWatch": "DATABASE_NAME=games_test jest --watch",
    "db:migrate": "knex migrate:latest",
    "db:rollback": "knex migrate:rollback"
  },
  "dependencies": {
    "jest": "^26.6.3",
    "knex": "^0.21.14",
    "pg": "^8.5.1"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/node_modules/",
      "__tests__/helpers/"
    ]
  }
}
