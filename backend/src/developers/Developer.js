class Developer {
  constructor({ id, name, foundedDate, logoUrl }) {
    this.id = id;
    this.name = name;
    this.foundedDate = foundedDate;
    this.logoUrl = logoUrl;
  }
}

module.exports = Developer;
